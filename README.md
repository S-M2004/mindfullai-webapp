# mindfullai-webapp

## Getting Started

1. Clone the repository using `git clone https://github.com/penguineo/mindfullai-webapp.git`.
2. Install the dependencies using `cd backend && npm i`.
3. Run the backend using `npm run dev`.
